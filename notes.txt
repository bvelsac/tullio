op  letten:


jQuery.ajax({
         url:    "/exist/tullio/xml/titles.xml",
         success: function(result) {
                      testeee = (new XMLSerializer()).serializeToString(result); 
											console.log(testeee);
											titlesAsString = testeee;
											titlesDoc = result;
                  },
         async:   false,
				 contentType: "text/xml"
    }); 
		ASYNC: anders is resultaat niet beschikbaar voor regels vlak erna
contentType: xml anders is het geen xml object

serializeren nr text: via method in core js, browser dependent
====> XML bestand moet juiste content type krijgen van server:

in controller.xql
deze config Wordt blijkbaar niet gecached
 kan gelden voor subdirs enz