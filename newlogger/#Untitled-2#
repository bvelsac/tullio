Pranav Hosangadi May 6, 2012 at 3:52 pm I’ve fixed the paragraph issue and the “does not count words on a single line” problem. Thought you guys would like it. Find the function GetWordCount in the plugin.js and change it to this:/**
* Takes the given HTML data, replaces all its HTML tags with nothing, splits the result by spaces,
* and outputs the array length i.e. number of words.
*
* @param string htmlData HTML Data
* @return int Word Count
*/
function GetWordCount(htmlData) {
//return htmlData.replace(//g, ”).split(‘ ‘).length;
var a=htmlData.replace(//g, ”);
a=a.replace(/(?: )/g, ‘ ‘);
a=a.replace(/\s+/g, ‘ ‘);
var b=trim12(a).split(‘ ‘);
return b.length;
} /**
* Trims leading and trailing whitespaces off the given string
*
* @param string str The String to be trimmed
* @return string trimmed string
*/
function trim12 (str) {
var	str = str.replace(/^\s\s*/, ”),
ws = /\s/,
i = str.length;
while (ws.test(str.charAt(–i)));
return str.slice(0, i + 1);
}
